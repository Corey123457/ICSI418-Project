Full program is in the releases section

To run it you just need to open a terminal in the app-name folder and enter "npm start"
if that doesn't work enter enter "run npm install" and then after it installs enter "npm start" and it should work
